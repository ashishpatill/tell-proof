# Peer plumbing reference (local only)

This file documents the gitignored pointer. The real JSON is
`research/plumbing-reference.local.json` (not committed).

Agents: if that JSON is missing, create it from the operator’s known peer
checkout or ask them to restore it — then follow
`.cursor/rules/tell-plumbing-reference.mdc` and `docs/11`–`docs/13`.

Never put the peer’s product name or URL into commits, code, or docs.
