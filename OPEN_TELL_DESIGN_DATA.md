# Open this folder

In the file explorer open:

**`tell-design-data/`**

(Cursor path: `/volumes/developer/workspace/tell-design-data`)

You should see `VISIBLE.txt`, `src/`, `README.md`, `package.json`.

If the folder is missing, pull branch `cursor/design-training-data-plan-3774`.

Sibling layout also prepared on the agent disk (may not map to your IDE):
`/volumes/developer/workspace/tell-proof`
`/volumes/developer/workspace/tell-design-data`
