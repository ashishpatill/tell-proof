node_modules/
dist/
.next/
.turbo/
coverage/
*.log
.env
.env.local
.DS_Store
playwright-report/
test-results/
*.tsbuildinfo

# Next generated
next-env.d.ts

# Generated capture/diagnose output
output/
tell-pr-report.json
tell-proof-compare.json
tell-proof-matrix.json
tell-preview-report.json

# Design research corpus: holds third-party URLs, never committed.
# Only anonymised measurements (research/measurements/*.json) are shipped.
research/corpus.local.json
research/raw/

# Training episode dumps must NOT live in Tell. Use the separate developer
# harness `tell-design-data` → ~/.tell-design-data/. Keep any accidental local
# drops out of git just in case:
research/training.local/

# Peer plumbing reference identity (URL + checkout path). Committed plans are
# anonymised in docs/11–13; never commit this file.
research/plumbing-reference.local.json

# Local MCP install backups
.cursor/mcp.json.bak
**/.cursor/mcp.json.bak

# Internal planning notes (kept local, not shipped)
docs/03_CLAUDE_DESIGN_BRIEF.md
docs/04_CLAUDE_PROJECT.md
.vercel
data/shared-reports/
research/shots/

# Throwaway probes written during a research loop to answer one question.
# Anything worth keeping graduates into scripts/design-research/.
scripts/tmp/

research/training.local/

