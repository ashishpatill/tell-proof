# Push to your private GitHub repo

```bash
cd tell-design-data
git push -u origin main
```

If needed: `gh auth login` then push again.

This folder is gitignored by Tell (`tell-proof`) and will not ship with the product.
