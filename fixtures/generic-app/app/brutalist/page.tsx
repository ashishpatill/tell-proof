export default function BrutalistRoute() {
  return (
    <main className="brutalist" data-intentional="mono">
      <p>Demo input: intentional mono route.</p>
      <h1>One typeface, on purpose.</h1>
      <p>
        Tell should not flag this as generic when facts show it is scoped to a deliberate route-level
        art direction.
      </p>
      <a href="/">Back to generic app</a>
    </main>
  );
}
